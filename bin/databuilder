#!/usr/bin/env php
<?php

if (is_file(__DIR__.'/../autoload.php') === true) {
    include_once __DIR__.'/../autoload.php';
}

$generator = new \Databuilder\DatabuilderGenerator();
$exitCode = $generator->generate();
exit($exitCode);